{
  "name": "phonebook-backend",
  "version": "0.0.1",
  "description": "phonebook backend",
  "license": "MIT",
  "author": "soon hong",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error no test script\" && exit 1",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../../part2/phonebook && npm run build && cp -r dist '../../part3/phonebook backend'",
    "build:full": "npm run build:ui && git add -A && git commit -m \"$MSG\" && git push"
  },
  "dependencies": {
    "express": "^5.2.1",
    "mongoose": "^9.9.4",
    "morgan": "^1.11.0"
  },
  "devDependencies": {
    "dotenv": "^17.4.2"
  }
}
